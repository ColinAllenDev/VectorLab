{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type":"cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/VL",
            "preLaunchTask": "build",
            "cwd": "${workspaceFolder}"
        }
    ]
}